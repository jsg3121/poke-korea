# Phase 3 베이스라인 — /type-effectiveness 청크 분할

> **측정 일자**: 2026-06-13
> **브랜치**: `feature/1.53.0-type-effectiveness-split`
> **개선 전 상태 스냅샷** — 작업 후 동일 측정으로 비교한다.

## 측정 환경
- `npm run build` 프로덕션 빌드 산출물 기준
- raw = 비압축 / gzip = 실제 전송 크기

---

## 1. 소스 파일 크기 (개선 대상)

| 파일 | 줄 수 | 바이트 |
| --- | --- | --- |
| desktop `TypeEffectivenessTable.component.tsx` | 1,041줄 | 75,878 B |
| mobile `TypeEffectivenessTable.component.tsx` | 1,043줄 | 75,977 B |
| desktop typeEffectiveness 컨테이너 전체 | 1,575줄 | 96,972 B |
| mobile typeEffectiveness 컨테이너 전체 | 1,586줄 | 97,255 B |

**근본 원인**: 18×18 타입 상성표를 JSX로 하드코딩. `<td>` 323개, `.map()` 2회.
각 셀에 긴 Tailwind className + `activeType` 조건부 클래스 반복 인라인.

---

## 2. 페이지 전용 청크 (핵심 비교 대상)

| 항목 | raw | gzip |
| --- | --- | --- |
| `app/type-effectiveness/page-*.js` | **173,931 B (169.9 KB)** | **16,934 B (16.5 KB)** |

> 이 청크가 1000줄 하드코딩 테이블이 만든 페이지 전용 번들이다.
> 반복 className이 많아 gzip 압축률이 높음(raw 대비 ~10%).

---

## 3. Next.js 빌드 출력 표기

| 라우트 | Size (page) | First Load JS |
| --- | --- | --- |
| `/type-effectiveness` | 16.8 kB | **180 kB** (전 라우트 중 최대) |
| `+ First Load JS shared by all` | - | 102 kB |

---

## 4. 페이지 로드 청크 합계 (참고)

- 총 11개 청크: **702.8 KB raw / 176.9 KB gzip**
- 이 중 공유 청크(4bd1b696, 1255 등)는 전 페이지 공통 → 비교 대상 아님
- **비교 핵심은 `page-*.js` 전용 청크 (위 2번)**

---

## 개선 목표
- 상성 데이터를 매트릭스로 추출 + 테이블 데이터-주도 렌더링
- 시각적 결과는 픽셀 단위로 동일 유지 (회귀 없음)
- `page-*.js` 전용 청크 raw/gzip 감소 측정

## 작업 후 비교 표 (작업 완료 시 채움)

| 측정 항목 | 개선 전 | 개선 후 | 절감 |
| --- | --- | --- | --- |
| desktop Table 소스 | 1,041줄 / 75,878 B | TBD | TBD |
| mobile Table 소스 | 1,043줄 / 75,977 B | TBD | TBD |
| page 전용 청크 (raw) | 173,931 B | TBD | TBD |
| page 전용 청크 (gzip) | 16,934 B | TBD | TBD |
| First Load JS | 180 kB | TBD | TBD |
